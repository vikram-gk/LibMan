please enter the following in the terminal after going into the mychat file

$ sudo apt install express --save
$ sudo apt install socket.io --save
$ sudo apt install ejs --save

then go to the terminal and input 

$ node index.js

u will see "server running"

then go to "http://localHost:3000"
